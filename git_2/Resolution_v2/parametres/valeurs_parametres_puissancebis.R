#Valeurs des paramètres définissant les fonctions des trades-off selon notre expression "puissance"
beta0=0
beta_max=2
P_beta=1

a0=0
a_max=10
P_a=2

alpha0=0
alpha_max=5
P_alpha=2

gamma0=0
gamma_max=1
P_gamma=0.5

b0=0.2
b_max=1
P_b=0.5

K=10

param_to <- list(a0=a0, alpha0=alpha0, alpha_max=alpha_max, a_max=a_max, b0=b0, beta0=beta0, beta_max=beta_max, b_max=b_max, gamma0=gamma0, gamma_max=gamma_max, K=K, P_a=P_a, P_alpha=P_alpha, P_b=P_b, P_beta=P_beta, P_gamma=P_gamma)
